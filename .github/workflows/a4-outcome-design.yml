name: A4 Outcome Design

on:
  pull_request:
    paths:
      - 'docs/dtc/DTC_A4_CAREER_DECISION_QUALITY_DESIGN.md'
      - 'scripts/check-a4-outcome-design.ts'
      - '.github/workflows/a4-outcome-design.yml'

jobs:
  a4-outcome-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec tsx scripts/check-a4-outcome-design.ts
