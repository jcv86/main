# Allow build scripts for safe dependencies
# sharp: Image processing library (widely used and safe)
# unrs-resolver: Resolver utility (safe)
allow-scripts=true

# Enable optional dependencies
optional-dependencies=true
